bool isOneBitCharacter(int* bits, int bitsSize) {
    int start;
    for(int i=0;i<bitsSize;i++){
        start=i;
        if(bits[i]){
            i++;
        }
    }
    if(start==bitsSize-1) return true;
    return false;
    
}
